# Development / test dependencies. Installs the runtime deps too, so a clean
# checkout can run the full offline suite with a single command:
#
#     pip install -r requirements-dev.txt
#     pytest -m "not live"
#
-r requirements.txt

pytest>=8.0
# 0.23 is the first release whose asyncio_mode="auto" is stable under pytest 8/9
# (the repo previously pinned nothing and ran on a legacy 0.18 install).
pytest-asyncio>=0.23
coverage[toml]>=7.0
