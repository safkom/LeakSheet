services:
  api:
    build: .
    restart: unless-stopped
    env_file:
      - path: .env
        required: false
    volumes:
      - cache:/app/.cache

  web:
    build: ./web
    restart: unless-stopped
    ports:
      - "8081:80"
    depends_on:
      - api

volumes:
  cache:
